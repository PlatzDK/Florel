SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

ADMIN_EMAIL=booking@example.com

API_BASE_URL=https://your-api-domain.com
SITE_URL=https://platzdk.github.io/Florel

PORT=3000

ALLOWED_ORIGINS=https://platzdk.github.io

# Optional: a short code word shared verbally with trusted guests.
# When set, every booking submission must include the correct code or it is rejected.
# Leave unset (or empty) to disable the access code gate.
BOOKING_ACCESS_CODE=
